name: iso_mobile
description: ИСО — Иммунная Система Общения. Мобильный клиент.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.0
  http: ^1.1.0
  provider: ^6.0.0
  intl: ^0.18.0
  multicast_dns: ^0.3.2
  shared_preferences: ^2.2.0
  flutter_blue_plus: ^2.3.12
  permission_handler: ^11.3.0
  uuid: ^4.3.3
  crypto: ^3.0.3
  flutter_secure_storage: ^9.0.0
  connectivity_plus: ^6.0.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true